// Insight Service entry point.
//
// Placeholder scaffold for task 1.1. The HTTP server, scheduler wiring, and
// JSON API (see design.md "index.ts" and "JSON API Design") are implemented in
// later tasks (5.12, 7.11). This file exists so the project compiles from the
// start with no orphaned code.

export const SERVICE_NAME = "insight-service";

export function main(): void {
  // Intentionally empty for now; server bootstrap is added in task 5.12.
}
